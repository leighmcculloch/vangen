builds:
  - binary: vangen
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64

archives:
  - format: tar.gz
    files:
      - LICENSE
    format_overrides:
    - goos: windows
      format: zip
